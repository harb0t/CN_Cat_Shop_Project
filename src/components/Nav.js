// import { ShopCart } from './ShopCart';
// import { Link } from 'react-router-dom';

const Navbar = ({ basketItems, removeItemFromBasket }) => {
	return (
		<div className="nav">
{/* link to the homepage and shopcart component ? set up shop basket component to be able to add or remove itemss */}
		</div>
	);
};

export default Navbar;